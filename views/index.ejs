<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <% if(blog.length > 0 ) { blog.forEach(b=>{%>

    <h1><%=b.b%><br /></h1>

    <%})}%>
    <h1>hi</h1>
  </body>
</html>
